<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicolas.brapi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginInPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PickADatabase"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SelectACategory"
            android:parentActivityName=".PickADatabase" />
        <activity
            android:name=".CallToURL"
            android:parentActivityName=".SearchADatabase" />
        <activity
            android:name=".SearchADatabase"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Register"
            android:parentActivityName=".LoginInPage" />
        <activity
            android:name=".AddADatabase"
            android:parentActivityName=".PickADatabase" />
        <activity android:name=".WebViewActivity" />
        <activity
            android:name=".RemoveADatabase"
            android:parentActivityName=".PickADatabase" />
        <activity android:name=".WebViewRegister" />
        <activity android:name=".WebViewLogin"></activity>
    </application>

</manifest>